# 2026-07-07｜Day 01｜链表重启

今天是签证事务后重新开始刷题的第一天。不要追求一口气补完前面欠下的题，先恢复节奏。

## 今日目标

- 主考点：链表入门。
- 训练核心：先理解链表数据结构，再做快慢指针。
- 今日底线：先能解释 `ListNode`、`head`、`next`、`cur`，再尝试 141。
- 纪律：没有理解链表基本操作前，不直接刷链表题。

## 2 小时安排

| 时间 | 内容 | 要求 |
| --- | --- | --- |
| 10m | 重启检查 | 写下今天最容易犯的一个错误 |
| 35m | 链表预备课 | 理解节点、指针、`head`、`next`、遍历、空链表 |
| 20m | 手动画链表 | 画出 `1 -> 2 -> 3 -> None`，并标出 `cur = cur.next` 的变化 |
| 25m | [141. 环形链表](https://leetcode.cn/problems/linked-list-cycle/) | 只做判断有无环，先不碰 21 和 2 |
| 20m | 141 复盘 | 解释为什么 `pos` 不是输入参数，为什么快慢指针能判断环 |
| 20m | 备用加练 | 状态好再看 [21. 合并两个有序链表](https://leetcode.cn/problems/merge-two-sorted-lists/) 的题意，不强制写完 |
| 10m | 复盘 | 写下今天学到的 1 个可复用模式 |

## 链表题写代码前必须回答

1. 一个节点由哪两部分组成？
2. `head` 是整个链表，还是第一个节点的引用？
3. `cur = cur.next` 之后，`cur` 指向哪里？
4. 如果 `cur` 是 `None`，还能不能访问 `cur.next`？
5. 这题是只遍历链表，还是要修改链表连接？
6. 如果要修改连接，有没有断链风险？

## 链表最小知识清单

今天先掌握这些，不跳级：

- `ListNode` 是节点，不是数组。
- 节点通常有两个字段：`val` 和 `next`。
- `head` 指向第一个节点；如果链表为空，`head` 是 `None`。
- `node.next` 指向下一个节点；最后一个节点的 `next` 是 `None`。
- 链表不能用下标随机访问，只能从一个节点沿着 `next` 一步一步走。
- 141 里的 `pos` 是 LeetCode 后台构造测试用例用的，不会传给函数。

## 今日复盘模板

完成后填写：

- 完成题目：
- 卡住的地方：
- 今天真正学到的东西：
- 下次遇到链表题，第一步应该做什么：
- 需要我明天追问自己的问题：
